#include<iostream>
#include<vector>
#include<string>


using namespace std;


int main()
{
    vector<int> v1;
    //v1.push_back(1);
   // v1.push_back(2);
    vector<int> tmp;
    for(int i=0;i!=10;++i) {
        //tmp.push_back(10-i);
    }
    //heap_sort(tmp);
    //for(int i=0;i!=tmp.size();++i) {
       // cout<<tmp[i]<<endl;
   // }
   // vector<vector<int>> re=combinationSum(v1,3);
   /* for(int i=0;i!=re.size();++i) {
        for(int j=0;j!=re[i].size();++j) {
            cout<<re[i][j]<<" ";
        }
        cout<<endl;
    }*/
    //cout<<numeric_limits<long long>::min()<<endl;
    return 0;
}
